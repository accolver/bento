# 2026-03-04 — Code Audit & Fixes

## Code Audit (13 issues found and fixed)
Al requested a thorough code quality audit focused on state management and Remote AI functionality.

### Critical (3) — Fixed
1. **Race condition in `AiServiceController`** (`ai_providers.dart`) — `ref.watch()` after `await`. Moved watches before awaits.
2. **Memory leak in `LocalAiService`** (`local_ai_service.dart`) — generation lock was no-op, native resources not cleaned up. Added proper locking + dispose cleanup.
3. **SSH client reference leak** (`terminal_provider.dart`) — dangling timer callbacks, references not nullified. Fixed cleanup flow.

### High (4) — Fixed
4. Null safety in AI suggestion provider
5. `RemoteAiDetectionState` — missing try-catch, UI stuck in loading
6. `SSHConnectionNotifier` — changed to `@Riverpod(keepAlive: true)`
7. Blocking SharedPreferences I/O in remote AI config — moved off UI thread

### Medium (3) — Fixed
8. Silent exception swallowing in `_persistBlock()` — added logging with stack traces
9. Unbounded `_fullOutputCache` — bounded to 50 entries with LRU eviction
10. Inconsistent error codes — confirmed already snake_case

### Low (3) — Fixed
11. Dead `_getApiKey()` removed from `ai_service_factory.dart`
12. `debugPrint()` wrapped in `kDebugMode` guards
13. `kMaxOutputSizeBytes` documented

## Test Results
- All 1,023 tests passed (2 skipped — integration tests needing Flutter bindings)
- `flutter analyze` clean
- No regressions from fixes
